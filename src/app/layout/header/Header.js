import React, {Component} from 'react';
import './Style.scss';

class Header extends Component {
  render () {
    return (
      <header>
        <span>React App</span>
      </header>
    );
  }
}

export default Header;
