import React, {Component} from 'react';
import IndexRoute from '../../routes';

class Main extends Component {
  render () {
    return (
      <main>
        <IndexRoute/>
      </main>
    );
  }
}

export default Main;